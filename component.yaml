apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cbergstrom-example-frontend
  description: This is an example frontend component for Backstage.
  tags:
    - frontend
    - example
  annotations:
    backstage.io/techdocs-ref: url:https://github.com/callebstrom/example-entities
spec:
  type: website
  lifecycle: production
  owner: team-a
  system: example-system
  consumesApis:
    - example-api
